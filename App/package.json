{
  "name": "praise-prayer-app",
  "version": "1.0.0",
  "scripts": {
    "upload:mbern": "aws s3 cp --recursive --profile mattbreed ./Public/ s3://stpetersbaptist-prayerwall/public/",
    "upload": "aws s3 cp --recursive --profile default ./Public/ s3://stpetersbaptist-prayerwall/public/"
  }
}
